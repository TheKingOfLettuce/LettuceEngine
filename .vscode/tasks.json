{
  "tasks": [
    {
      "type": "shell",
      "label": "Build Static Lib",
      "command": "make",
      "args": [
        "BuildStaticLib"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "detail": "Build the LettuceEngine library"
    },
    {
      "type": "shell",
      "label": "Clean Build Files",
      "command": "make",
      "args": [
        "clean"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "detail": "Clean the build files"
    }
  ],
  "version": "2.0.0"
}